name = "weekend-ai-proxy"
main = "worker.js"
compatibility_date = "2024-11-01"

# 허용할 사이트 주소(선택). GitHub Pages 주소로 바꾸면 그 사이트에서만 호출됩니다.
# 비워두거나 "*"로 두면 모든 출처를 허용합니다.
[vars]
ALLOWED_ORIGIN = "*"

# ANTHROPIC_API_KEY 는 코드/설정에 적지 말고 Secret 으로 넣으세요:
#   npx wrangler secret put ANTHROPIC_API_KEY
