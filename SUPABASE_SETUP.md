# 📸 또또 가족 갤러리 — Supabase 설정 가이드

프로젝트: `https://supabase.com/dashboard/project/cuxcxzqfcnofmuusxsvo`

전체 3단계, 약 5분이면 끝납니다.

---

## 1단계 · DB / 권한 / 저장소 만들기 (SQL 한 번 실행)

1. 대시보드 → 왼쪽 메뉴 **SQL Editor** → **New query**
2. 저장소의 [`supabase/schema.sql`](supabase/schema.sql) 파일 내용을 **전부 복사해서 붙여넣기**
3. 오른쪽 아래 **RUN** (또는 `Ctrl/Cmd + Enter`)
4. `Success. No rows returned` 이 나오면 정상입니다

이 SQL 하나가 아래를 전부 만듭니다.

| 만들어지는 것 | 설명 |
|---|---|
| `profiles` | 가족 구성원 + 권한(`admin`/`parent`/`family`/`gallery_only`) |
| `albums` | 앨범 (기본 7개 자동 생성: 탄생·우리집·50일·100일·봄나들이·첫 크리스마스·첫돌) |
| `media` | 사진/영상 메타데이터 (원본·감상용·썸네일 3단계 경로) |
| `family-media` 버킷 | **비공개** 저장소 (Public = OFF 강제) |
| RLS 정책 | 아래 표 참고 |
| 가입 트리거 | 새로 가입하면 자동으로 `gallery_only` + 승인대기 |

> ⚠️ SQL 안에 총관리자 이메일이 `scott7259@naver.com` 으로 적혀 있습니다.
> 다른 이메일로 쓰시려면 `handle_new_user()` 함수의 `bootstrap` 값을 바꾼 뒤 실행하세요.

---

## 2단계 · anon 키 복사해서 붙여넣기

1. 대시보드 → **Project Settings** → **API Keys**
2. **`anon` / `public`** 항목의 긴 문자열(`eyJ...`) 복사
3. 저장소의 [`supabase-config.js`](supabase-config.js) 파일을 열어 붙여넣기:

```js
window.SUPABASE_URL = "https://cuxcxzqfcnofmuusxsvo.supabase.co";  // 이미 채워져 있음
window.SUPABASE_ANON_KEY = "eyJhbGciOi...여기에 붙여넣기...";       // ← 이것만 채우면 끝
```

4. 아기 정보도 같이 채워주세요 (액자 모드에서 `또또 · D+132` 표시에 사용):

```js
window.BABY_INFO = { name: "또또", birthDate: "2026-05-01" };
```

### 🔑 키에 대해 꼭 알아두실 것

| 키 | 프론트엔드에 넣어도 되나 | 이유 |
|---|---|---|
| **`anon` / `public`** | ✅ **넣는 것이 정상** | 공개되도록 설계된 키. 실제 보안은 RLS가 담당 |
| **`service_role`** | ⛔ **절대 금지** | RLS를 통째로 무시. 유출되면 가족 사진 전체가 열림 |

---

## 3단계 · 첫 로그인 & 가족 초대

1. `https://scott910512-source.github.io/toto/gallery.html` 접속
2. **가입하기** → 총관리자 이메일(`scott7259@naver.com`)로 가입 → 자동으로 `admin` + 승인됨
3. 가족들에게 같은 주소를 알려주고 각자 가입하게 함
4. 관리자로 로그인 → 하단 **👥 가족** → 각자 **승인** 누르고 권한 지정

### 권한표

| 역할 | 갤러리 보기 | 원본 저장 | 슬라이드쇼 | 사진 올리기 | 앨범 관리 | 가족 관리 |
|---|:---:|:---:|:---:|:---:|:---:|:---:|
| `admin` (아빠) | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| `parent` (엄마) | ✅ | ✅ | ✅ | ✅ | ✅ | ❌ |
| `family` (친척) | ✅ | ✅ | ✅ | ❌ | ❌ | ❌ |
| `gallery_only` (조부모) | ✅ | ✅ | ✅ | ❌ | ❌ | ❌ |

`gallery_only` 계정은 로그인하면 **[📸 사진 보기] [▶ 슬라이드쇼]** 두 개 버튼만 있는
초간단 화면으로 바로 들어갑니다. 주소창에 `#/upload`, `#/users` 를 직접 쳐도
**갤러리로 되돌려 보냅니다** (화면 가드 + DB RLS 이중 차단).

---

## 🖼 액자 모드 사용법 (아이패드 / 갤탭 / 안 쓰는 폰)

1. 기기 브라우저로 `gallery.html` 접속 → 로그인
2. **▶ 슬라이드쇼** → 왼쪽 위 **🖼 액자 모드**
3. 화면을 세워두면 끝. 사진 + 날짜 + `또또 · D+132` 만 남고 나머지 UI는 사라집니다
4. 재생 중에는 **Screen Wake Lock** 으로 화면이 자동으로 꺼지지 않습니다
   - 앱을 나갔다 돌아와도 자동으로 다시 잠금 해제를 요청합니다
   - 지원하지 않는 기기(구형 iOS 등)에서는 안내만 뜨니, 기기 설정에서
     **화면 자동 잠금 → 안 함** 으로 바꿔주세요
5. 홈 화면에 추가하면 주소창 없는 앱(`standalone`)으로 실행됩니다

슬라이드쇼 설정(⚙): 사진 선택(전체/즐겨찾기/최근/앨범) · 전환 시간(3·5·7·10·15·30초) ·
순서(최신/오래된/랜덤) · 효과(크로스페이드/페이드/슬라이드) · 화면 맞춤(전체 보기/채우기).
선택한 설정은 기기에 저장돼 다음에도 그대로 적용됩니다.

---

## 🔒 보안 구조 요약

- 저장소 버킷 `family-media` 는 **Public = OFF** (SQL이 강제로 꺼둠)
- 사진은 **1시간짜리 signed URL** 로만 전달 — 로그아웃/미승인 상태에서는 발급 자체가 안 됨
- 모든 테이블에 **RLS 적용**. 승인(`approved = true`)되지 않은 계정은 사진이 **0건** 조회됨
- 업로드는 `admin`/`parent` 만, 삭제는 **관리자 또는 올린 본인**만
- 본인이 스스로 권한을 올리는 것은 DB 트리거(`guard_profile_escalation`)가 차단
- 서비스워커가 **supabase.co 요청을 캐시하지 않음** → 기기에 사진·토큰이 남지 않음
- 로그아웃 시 저장된 이메일·세션·서명 URL 캐시를 모두 정리

---

## ❓ 문제가 생기면

| 증상 | 원인 / 해결 |
|---|---|
| "Supabase 연결이 필요해요" 화면 | 2단계 anon 키가 비어 있음 |
| 로그인은 되는데 "승인을 기다리고 있어요" | 정상. 관리자가 **👥 가족**에서 승인해야 함 |
| 사진이 안 보임 (0장) | 승인 안 됨, 또는 1단계 SQL 미실행 |
| 업로드 시 "권한이 없어요" | 역할이 `family`/`gallery_only` — `parent` 이상 필요 |
| HEIC 사진 썸네일이 안 생김 | 브라우저가 HEIC를 못 여는 경우. **원본은 정상 업로드되어 있고** 저장도 됩니다 |
